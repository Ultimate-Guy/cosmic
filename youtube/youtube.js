const YT_SEARCH_URL='https://ytsearch.leelive2021.workers.dev/';
const videoInput=document.getElementById('videoUrlInput'),shortInput=document.getElementById('shortUrlInput'),videoList=document.getElementById('videoList'),videoEmpty=document.getElementById('videoEmpty'),shortPlayer=document.getElementById('shortPlayer'),shortCounter=document.getElementById('shortCounter'),shortDots=document.getElementById('shortDots'),videoMessage=document.getElementById('videoMessage'),shortMessage=document.getElementById('shortMessage');
const searchInput=document.getElementById('searchInput'),searchBtn=document.getElementById('searchBtn'),searchResults=document.getElementById('searchResults'),searchResultsSection=document.getElementById('searchResultsSection'),searchStatus=document.getElementById('searchStatus'),resultCount=document.getElementById('resultCount');let shorts=[],shortIndex=0;
function extractYouTubeId(value){try{const raw=value.trim();if(!raw)return null;const u=new URL(raw);if(u.hostname==='youtu.be')return u.pathname.split('/').filter(Boolean)[0]||null;if(u.hostname.includes('youtube.com')){if(u.pathname==='/watch')return u.searchParams.get('v');const parts=u.pathname.split('/').filter(Boolean);if(parts[0]==='shorts'||parts[0]==='embed')return parts[1]||null}}catch(e){}const m=value.match(/(?:youtu\.be\/|youtube\.com\/(?:watch\?v=|shorts\/|embed\/))([\w-]{11})/i);return m?m[1]:null}
function validId(id){return !!id&&/^[\w-]{11}$/.test(id)}function embedUrl(id){return `https://www.youtube-nocookie.com/embed/${encodeURIComponent(id)}?rel=0&playsinline=1`}function setMessage(el,text,error=false){el.textContent=text;el.style.color=error?'#ff6b6b':''}
function addVideo(){const id=extractYouTubeId(videoInput.value);if(!validId(id)){setMessage(videoMessage,'Enter a valid YouTube video URL.',true);return}setMessage(videoMessage,'');const unit=document.createElement('article');unit.className='video-unit';unit.dataset.videoId=id;const frame=document.createElement('div');frame.className='video-frame';const iframe=document.createElement('iframe');iframe.src=embedUrl(id);iframe.title='YouTube video';iframe.allow='accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share';iframe.allowFullscreen=true;iframe.loading='lazy';frame.appendChild(iframe);const tools=document.createElement('div');tools.className='video-tools';const close=document.createElement('button');close.type='button';close.textContent='Close';close.onclick=()=>{unit.remove();updateVideoEmpty()};const open=document.createElement('button');open.type='button';open.textContent='Open YouTube';open.onclick=()=>window.open(`https://www.youtube.com/watch?v=${id}`,'_blank','noopener');tools.append(open,close);unit.append(frame,tools);videoList.prepend(unit);videoEmpty.style.display='none';videoInput.value='';saveVideos();document.getElementById('watchPanel').scrollIntoView({behavior:'smooth',block:'start'})}
function watchSearchResult(id){if(!validId(id))return;videoInput.value=`https://www.youtube.com/watch?v=${id}`;addVideo()}
function updateVideoEmpty(){videoEmpty.style.display=videoList.children.length?'none':'flex';saveVideos()}function saveVideos(){localStorage.setItem('cosmicYoutubeVideos',JSON.stringify([...videoList.querySelectorAll('.video-unit')].map(x=>x.dataset.videoId)))}function loadVideos(){try{const ids=JSON.parse(localStorage.getItem('cosmicYoutubeVideos')||'[]');ids.reverse().forEach(id=>{if(validId(id)){videoInput.value=`https://www.youtube.com/watch?v=${id}`;addVideo()}});videoInput.value=''}catch(e){}}function clearVideos(){videoList.innerHTML='';videoEmpty.style.display='flex';localStorage.removeItem('cosmicYoutubeVideos')}
async function searchYouTube(){const query=searchInput.value.trim();if(!query){searchResultsSection.classList.add('hidden');searchStatus.textContent='';return}searchResultsSection.classList.remove('hidden');searchResults.innerHTML=Array(8).fill('<div class="result-card skeleton-card"><div class="skeleton-thumb"></div><div class="skeleton-line"></div><div class="skeleton-line short"></div></div>').join('');resultCount.textContent='';searchStatus.textContent='Searching YouTube...';try{const response=await fetch(`${YT_SEARCH_URL}?q=${encodeURIComponent(query)}`);if(!response.ok)throw new Error(`HTTP ${response.status}`);const result=await response.json();const items=Array.isArray(result.data?.items)?result.data.items:[];renderSearchResults(items);searchStatus.textContent=items.length?'':'No results found.'}catch(error){console.error('Cosmic YouTube search error:',error);searchResults.innerHTML='<div class="search-error"><i class="fas fa-triangle-exclamation"></i><strong>Search unavailable</strong><span>The YouTube search service did not respond. You can still paste a YouTube URL below.</span></div>';searchStatus.textContent='Search error'}}
function renderSearchResults(items){searchResults.innerHTML='';resultCount.textContent=`${items.length} result${items.length===1?'':'s'}`;items.forEach(item=>{const id=typeof item.id==='string'?item.id:item.id?.videoId;const snippet=item.snippet||{};if(!validId(id))return;const title=snippet.title||'Untitled video',channel=snippet.channelTitle||'YouTube',thumb=snippet.thumbnails?.high?.url||snippet.thumbnails?.medium?.url||snippet.thumbnails?.default?.url||'';const card=document.createElement('button');card.type='button';card.className='result-card';card.title=`Watch ${title}`;card.innerHTML=`<div class="result-thumb">${thumb?`<img src="${thumb}" loading="lazy" alt="">`:''}<span class="result-play"><i class="fas fa-play"></i></span></div><div class="result-info"><div class="result-title"></div><div class="result-channel"></div></div>`;card.querySelector('.result-title').textContent=title;card.querySelector('.result-channel').textContent=channel;card.onclick=()=>watchSearchResult(id);searchResults.appendChild(card)})}
function addShort(){const id=extractYouTubeId(shortInput.value);if(!validId(id)){setMessage(shortMessage,'Enter a valid YouTube Shorts URL.',true);return}if(!shorts.includes(id))shorts.push(id);shortIndex=shorts.length-1;shortInput.value='';setMessage(shortMessage,'');renderShort();saveShorts()}function renderShort(){if(!shorts.length){shortPlayer.className='short-player empty-short';shortPlayer.innerHTML='<div class="empty-short-content"><i class="fas fa-mobile-screen-button"></i><strong>No Shorts loaded</strong><span>Add a Shorts URL above.</span></div>';shortCounter.textContent='0 Shorts';shortDots.innerHTML='';return}shortIndex=(shortIndex+shorts.length)%shorts.length;shortPlayer.className='short-player';shortPlayer.innerHTML='';const iframe=document.createElement('iframe');iframe.src=embedUrl(shorts[shortIndex]);iframe.title=`YouTube Short ${shortIndex+1}`;iframe.allow='accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share';iframe.allowFullscreen=true;shortPlayer.appendChild(iframe);shortCounter.textContent=`Short ${shortIndex+1} of ${shorts.length}`;shortDots.innerHTML='';shorts.forEach((_,i)=>{const b=document.createElement('button');b.className='short-dot'+(i===shortIndex?' active':'');b.type='button';b.title=`Short ${i+1}`;b.onclick=()=>{shortIndex=i;renderShort();saveShorts()};shortDots.appendChild(b)})}function nextShort(){if(shorts.length){shortIndex=(shortIndex+1)%shorts.length;renderShort();saveShorts()}}function prevShort(){if(shorts.length){shortIndex=(shortIndex-1+shorts.length)%shorts.length;renderShort();saveShorts()}}function saveShorts(){localStorage.setItem('cosmicYoutubeShorts',JSON.stringify(shorts))}function loadShorts(){try{const saved=JSON.parse(localStorage.getItem('cosmicYoutubeShorts')||'[]');if(Array.isArray(saved))shorts=saved.filter(validId);renderShort()}catch(e){renderShort()}}function clearShorts(){shorts=[];shortIndex=0;localStorage.removeItem('cosmicYoutubeShorts');renderShort()}
function setMode(shortMode){document.getElementById('watchPanel').classList.toggle('hidden',shortMode);document.getElementById('shortsPanel').classList.toggle('hidden',!shortMode);document.getElementById('watchModeBtn').classList.toggle('active',!shortMode);document.getElementById('shortsModeBtn').classList.toggle('active',shortMode)}
searchBtn.onclick=searchYouTube;searchInput.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();searchYouTube()}});document.getElementById('loadVideoBtn').onclick=addVideo;document.getElementById('addShortBtn').onclick=addShort;document.getElementById('clearVideosBtn').onclick=clearVideos;document.getElementById('clearShortsBtn').onclick=clearShorts;document.getElementById('nextShortBtn').onclick=nextShort;document.getElementById('prevShortBtn').onclick=prevShort;document.getElementById('watchModeBtn').onclick=()=>setMode(false);document.getElementById('shortsModeBtn').onclick=()=>setMode(true);videoInput.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();addVideo()}});shortInput.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();addShort()}});document.addEventListener('keydown',e=>{if(document.getElementById('shortsPanel').classList.contains('hidden'))return;if(e.key==='ArrowDown')nextShort();if(e.key==='ArrowUp')prevShort()});loadVideos();loadShorts();